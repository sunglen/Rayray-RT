/*
 * This file was automatically generated by tecsgen.
 * Move and rename like below before editing,
 *   gen/tSample_templ.c => src/tSample.c
 * to avoid to be overwritten by tecsgen.
 */
/* #[<PREAMBLE>]#
 * Don't edit the comments between #[<...>]# and #[</...>]#
 * These comment are used by tecsmerege when merging.
 *
 * #[</PREAMBLE>]# */

/* Put prototype declaration and/or variale definition here #_PAC_# */
#include "tSample_tecsgen.h"
#include "kernel_cfg.h"

#include <stdio.h>
#include <string.h>

#include "tFileSystem.h"

#ifndef E_OK
#define	E_OK	0		/* success */
#define	E_ID	(-18)	/* illegal ID */
#endif

/* entry port function #_TEPF_# */
/* #[<ENTRY_PORT>]# eEnt
 * entry port: eEnt
 * signature:  sSample
 * context:    task
 * #[</ENTRY_PORT>]# */

/* #[<ENTRY_FUNC>]# eEnt_sayHello
 * name:         eEnt_sayHello
 * global_name:  tSample_eEnt_sayHello
 * oneway:       false
 * #[</ENTRY_FUNC>]# */
ER
eEnt_sayHello(CELLIDX idx, int32_t times)
{
	ER		ercd = E_OK;
	CELLCB	*p_cellcb;
	if (VALID_IDX(idx)) {
		p_cellcb = GET_CELLCB(idx);
	}
	else {
		return(E_ID);
	} /* end if VALID_IDX(idx) */

	/* Put statements here #_TEFB_# */
	printf( "Sample: " );
	while( times-- > 0 )
		printf( "hello " );
	printf( "\n" );

    int32_t fd;
    const char *path="/rSample.txt";
    uint32_t count;
    const char p_buffer[] = "FileSystem is OK.\n";

    //cFileSystem_config();
    cFileSystem_openFile(&fd, path, O_WRONLY|O_CREAT|O_TRUNC);
    cFileSystem_seek(fd, &count, 0);
    cFileSystem_writeFile(fd, p_buffer, &count, sizeof(p_buffer));
    cFileSystem_closeFile(fd);
    printf( "%d byte(s) saved.\n", count);

	return(ercd);
}

/* #[<ENTRY_FUNC>]# eEnt_howAreYou
 * name:         eEnt_howAreYou
 * global_name:  tSample_eEnt_howAreYou
 * oneway:       false
 * #[</ENTRY_FUNC>]# */
ER
eEnt_howAreYou(CELLIDX idx, char_t* buf, int32_t len)
{
	ER		ercd = E_OK;
	CELLCB	*p_cellcb;
	if (VALID_IDX(idx)) {
		p_cellcb = GET_CELLCB(idx);
	}
	else {
		return(E_ID);
	} /* end if VALID_IDX(idx) */

	/* Put statements here #_TEFB_# */
	//strncpy( buf, "Sample: I'm fine!", len );

    int32_t fd;
    const char *path="/rSample.txt";
    uint32_t count;

    //cFileSystem_config();
    cFileSystem_openFile(&fd, path, O_RDONLY);
    cFileSystem_readFile(fd, buf, &count, len);
    cFileSystem_closeFile(fd);

	return(ercd);
}

/* #[<POSTAMBLE>]#
 *   Put non-entry functions below.
 * #[</POSTAMBLE>]#*/
